project('nums', 'c',
  license : 'GPL-2.0-or-later',
  version : '0.1',
  default_options : 'c_std=gnu99')

gnome = import('gnome')

deps = [
  dependency('gtk4'),
  dependency('cgs')
]

subdir('src')

